name: kingslayer_chess
description: "IdeaSpace Chess Academy - A professional chess training application."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  chess: ^0.8.1
  flutter_riverpod: ^2.5.1
  flutter_svg: ^2.0.10+1
  path_provider: ^2.1.3
  uuid: ^4.4.0
  path: ^1.9.0
  http: ^1.2.0
  stockfish_chess_engine: ^0.8.2
  flutter_soloud: ^3.2.7
  google_fonts: ^6.2.1
  intl: ^0.19.0
  share_plus: ^12.0.2
  rust_lib_kingslayer_chess:
    path: rust_builder
  flutter_rust_bridge: 2.12.0
  shared_preferences: ^2.5.5
  llama_cpp_dart: ^0.9.0-dev.6
  fl_chart: ^1.2.0
  confetti: ^0.7.0
  image_picker: ^1.1.2
  file_picker: ^8.0.0
  video_player: ^2.11.1
  chess_assets:
    path: packages/chess_assets
  archive: ^4.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1
  integration_test:
    sdk: flutter

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/splash/appicon.png"
  adaptive_icon_background: "#F7F7EF"
  adaptive_icon_foreground: "assets/splash/appicon_foreground.png"

flutter:
  uses-material-design: true

  assets:
    - assets/pieces/
    - assets/pieces/classic_svg/
    - assets/pieces/fairytale_castle/
    - assets/pieces/bubblegum/
    - assets/pieces/copper/
    - assets/pieces/goldnsilver/
    - assets/pieces/marble/
    - assets/pieces/sandmud/
    - assets/pieces/silky/
    - assets/pieces/summernautumn/
    - assets/pieces/woodyy/
    - assets/pieces/diamonds/
    - assets/pieces/diamonds-webP/
    - assets/pieces/lightening/
    - assets/pieces/lightening-webP/
    - assets/pieces/royal/
    - assets/pieces/rootpieces/
    - assets/pieces/energy/
    - assets/pieces/energy-webP/
    - assets/pieces/forrest/
    - assets/pieces/arc/
    - assets/pieces/arc-webP/
    - assets/persona/
    - assets/sfx/
    - assets/bgm/
    - assets/splash/
    - assets/engine/
    # - assets/engine/wincessengines/stockfish-windows-x86-64-avx2/stockfish/
    # - assets/engine/wincessengines/stockfish-windows-x86-64/stockfish/
    # - assets/engine/wincessengines/crafty/
    - assets/data/
    - assets/prescriptions/
    - assets/llm/
    - assets/llm/gemma/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key for a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

